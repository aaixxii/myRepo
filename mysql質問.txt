Got an error reading communication packets


  final InitialContext ctx = new InitialContext();
  final SessionContext sctx = (SessionContext) ctx.lookup("java:comp/env/sessioncontext");



1．mysql proceduce中mysqlのエラログ出力できますか？
2. mysql procedurceにTEMPORARY TABLEを使っています。これが禁止ではないですね？
　ただし、たまには、エラが発生してMySQLがTEMPORARY TABLEがみつからないとエラーメッセージがでる。
2．mysql 稼働時間長くなると、処理が遅くなってます。何か対策がありますか？
3．業務では、全テーブルのlock、或いは一部のlock必要ですが、
　　mysqlのlockの動作はOracleと違いますね、lock掛けると、lock取得できない側はすぐ素直にエラーが吐くてしまった。
4. テーブルにデータが存在していますが、mysql workbench上でもデータが見える、lockもかけていない、
　　commitもしている状態なのに、mysqlはデータが取得できない場合もある。

３．MySQLはよくDeadlockが発生してしまう。ただし、発生しても処理がつづけられる。
　　これについて、プログラムを書くとき、何か注意すべきですか？


5. すごくXAトランザクションに依存するOralce製品、そして業務も９０％プロシージャでできる製品、
　　本当にMySQLにきりかえられますか、私もNoと思っています。

6. java.lang.RuntimeException: WFLYCTL0079: Failed initializing module org.jboss.as.logging

Caused by: com.nec.aim.uid.amqclient.exception.XmlException: Error on line 1 of document  : プロローグにはコンテンツを指定できません。 Nested exception: プロローグにはコンテンツを指定できません



　　

　　
　　
　　
　